name: Build LaTeX PDF

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Compile LaTeX document
        uses: xu-cheng/latex-action@v3
        with:
          root_file: main.tex
          extra_system_packages: "py-pygments"
          latexmk_shell_escape: true
          pre_compile: |
            tlmgr update --self
            tlmgr install elsarticle amscls amsmath amsfonts booktabs graphicx multirow xcolor algorithms algorithmicx hyperref cleveref subcaption siunitx tabularx rotating psnfss

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: paper-pdf
          path: main.pdf
          retention-days: 90

      - name: Commit PDF to repository
        if: github.event_name == 'push'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -f main.pdf
          git diff --cached --quiet || git commit -m "Auto-build: update PDF [skip ci]"
          git push
